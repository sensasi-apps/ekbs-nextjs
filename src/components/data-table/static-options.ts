const staticOptions = {
    filter: false,
    jumpToPage: true,
    print: false,
    responsive: 'standard' as const,
    searchDelay: 500,
    selectableRows: 'none' as const,
    serverSide: true,
}

export default staticOptions
