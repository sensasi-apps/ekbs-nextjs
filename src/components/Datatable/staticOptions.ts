const staticOptions = {
    filter: false,
    serverSide: true,
    responsive: 'standard' as const,
    selectableRows: 'none' as const,
    print: false,
    jumpToPage: true,
    searchDelay: 500,
}

export default staticOptions
